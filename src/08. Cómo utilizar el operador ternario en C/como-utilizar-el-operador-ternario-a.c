/*
 * Curso: Domina C
 * Plataforma: LinkedIn Learning
 * Instructor: @EliezerLopez
 * */

#include <stdio.h>

int main(){

    int a, b;

    // Ejemplo 1
    if(a > b) puts("Verdadero");
    else puts("Falso");

    // Ejemplo 2
    int menor;

    if(a < b) menor = a;
    else menor = b;

    return 0;
}