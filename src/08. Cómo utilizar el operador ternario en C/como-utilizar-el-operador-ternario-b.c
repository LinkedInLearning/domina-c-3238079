/*
 * Curso: Domina C
 * Plataforma: LinkedIn Learning
 * Instructor: @EliezerLopez
 * */

#include <stdio.h>

int main(){

    int a, b;

    // Ejemplo 1
    (a > b) ? puts("Verdadero") : puts("Falso");

    // Ejemplo 2
    int menor = ((a < b) ? a : b);

    return 0;
}